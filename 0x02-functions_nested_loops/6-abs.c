#include "main.h"

/**
 * _abs - returns the absolute of an integer
 * @n: a variable which is an integer
 *
 * Description: this function computes the absolute value of an integer
 * Return: the value of an integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
