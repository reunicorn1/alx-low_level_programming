#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n = 0;

	/* Printing numbers */
	for (n = 0; n < 10; n++)
		printf("%d", n);
	putchar('\n');
	return (0);
}
