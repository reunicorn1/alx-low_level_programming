#include "main.h"

/**
 * add - adds two integers and returns the result
 * @n: is a variable of int type
 * @m: is another variable of int type
 *
 * Description: this function adds two integers and returns the result
 * Return: returns the result
 */
int add(int n, int m)
{
	int result;

	result = n + m;
	return (result);
}
